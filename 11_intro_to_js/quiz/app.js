var name = parseInt(prompt ("What does GA stand for?"));

if (name == "General Assembly") {
  console.log("Correct!");
} else {
  console.log("Close, but not quite. The answer is General Assembly.");
}
